- apiVersion: v1
  kind: ResourceQuota
  metadata:
    name: compute-quota
    namespace: ${PROJECT_NAME}
  spec:
    hard:
      limits.cpu: "12"
      limits.memory: 24Gi
      persistentvolumeclaims: "100"
      requests.storage: 2Gi
